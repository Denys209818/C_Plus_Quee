#include "libs.h"
#include "Quee.h"
#include "RingQuee.h"

void main() 
{
	/*Quee<int> q;
	q.Push(5);
	q.Push(2);
	q.Push(3);
	q.Push(4);
    q.Pop();  
	int b = q.front();
	cout << "b - " << b << endl;
	cout << "To RingQuee press Enter:";
	_getch();

	RingQuee<int> r;
	r.Push(5);
	r.Push(4);
	r.Push(3);
	r.Push(2);
	r.Push(1);
	
	for (int i = 0; i < RingQuee<int>::col; i++) 
	{
		system("cls");
		cout << r.Pop() << endl;
		_getch();
	}*/



}